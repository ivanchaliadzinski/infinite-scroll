import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  header: {
    fontSize: 24,
    fontWeight: 'bold',
    margin: 16,
  },
  loader: {
    marginTop: 20,
  },
  container: {
    paddingBottom: 64,
  },
  center: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  footer: {
    marginVertical: 20,
  },
});
